import 'package:flutter/material.dart';

class AppTheme {
  static const bg = Color(0xFF1E2738); // Warna background utama aplikasi
  static const surface = Color(0xFF283548); // Warna permukaan/card
  static const surfaceHigh = Color(0xFF324158); // Warna permukaan terang (highlight)
  static const border = Color(0xFF3E4F6A); // Warna garis batas (border)
  static const accent = Color(0xFF638BFF); // soft blue
  static const accentSoft = Color(0xFF4FA0FF);
  static const atmColor = Color(0xFF638BFF); // soft blue
  static const bankColor = Color(0xFF8B9BB4); // slate
  static const userColor = Color(0xFFFF6B6B); // coral
  static const textPrimary = Colors.white; // Warna teks utama
  static const textMuted = Color(0xFF8B9BB4); // Warna teks redup/sekunder
  static const textDim = Color(0xFF6A7B96); // Warna teks lebih gelap (tersembunyi)

  // Semantic colors
  static const bullish = Color(0xFF26A69A); // Warna hijau (tren naik/bullish)
  static const bearish = Color(0xFFEF5350); // Warna merah (tren turun/bearish)
  static const warning = Color(0xFFF59E0B); // Warna kuning/oranye peringatan
}
